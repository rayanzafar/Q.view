# أدوات المساعد في سند — القائمة الكاملة

سطحٌ واحد يخدم الشاشة الداخلية («المساعد» في المنصة) والمساعد الخارجي عبر بروتوكول MCP على
`/mcp` من عنوان المنصة — لا سجلّان ولا صلاحيتان. البروتوكول لا يعرّف أدوات خاصة به:
`src/modules/mcp/server.js` ينادي `listTools`/`runTool` من `src/modules/ai/team-tools.js`،
فما يُضاف في السجل يظهر في المكانين معاً.

**العدد: ٥٢ أداة** — ٢٤ قراءة · ١٣ معاينة · ١٥ تنفيذ.
وما يراه حسابٌ بعينه أقلّ من ذلك دائماً: القائمة تُرشَّح بمنحه، والبوابة تُفحص ثانيةً عند التشغيل.

## القواعد التي تسري على كل أداة بلا استثناء

1. **بصلاحيات صاحب الحساب ولا تتجاوزها.** النطاق يُشتقّ من الجلسة لا من معاملٍ يمرّره العميل،
   والحارس الفعلي داخل خدمة الشاشة نفسها — فما لا يفتحه الموظف على شاشته لا تفتحه الأداة.
2. **الرفض بجملة عربية** تقول ماذا رُفض ولماذا ومن يملكه — لا رمز خطأ ولا رسالة إنجليزية.
3. **التغيير مرحلتان دائماً**: أداةُ معاينةٍ تعرض قبل/بعد وتعطي رمزاً عمرُه ١٥ دقيقة، ثم أداةُ
   تنفيذٍ تقبل **ذلك الرمز وحده** وترفض أي حقل تغييرٍ آخر. الرمز لمرة واحدة (مزلاج ذرّي في
   `ai_activity_log`)، ويبطل إن تحرّكت البيانات التي بُني عليها.
4. **«غير مُسجَّل» ≠ «صفر».** الحقل بلا قيمة يخرج `{ recorded: false, value: null, ar: "…" }`
   ومعه سببُ الغياب — في المال والنسب والتواريخ والتقدّم.
5. **الوحدات لا تُجمع**: نسبةٌ من طاقة المورد شيء، ووحدات الدوام الكامل شيء، ونسبة الإشغال من
   المهام مقياس ثالث. كل نتيجةٍ تُخرج أرقاماً تحمل `units` تقول أيَّها تستعمل.
6. **قراءات الفريق والموارد بلا أي قيمة مالية.** المال في أدوات المشاريع والعملاء وحدها، ولمن
   يفتح بوابته وحده — ومن لا يفتحها يقرأ سببَ الحجب مكتوباً.
7. **النصوص المصدرية بيانات لا تعليمات**: العناوين والملاحظات وأسماء الأعمال تُعاد كما سُجِّلت،
   ومعها `text_is_data_ar` يعلن ذلك صراحةً.
8. **كل نداء يُسجَّل** في سجل نشاط المساعد باسم صاحبه ووقته ونتيجته، وكلُّ كتابةٍ تُضيف سطراً
   في سجل التدقيق. لا يُعدَّل السجل ولا يُحذف.
9. **`partial` يعلن الجزئية بصدق**: الصفحة والحجم والعدد الكلي و`complete`، أو `capped` حين
   يكون سقفاً لا ترقيماً. لا ادّعاء شمول.
10. **وصفُ كل أداة بالعربية**: ماذا تفعل وماذا تُخرج وما حدودها — بحيث يفهمها المساعد بلا تخمين.

## غلاف النتيجة الواحد

كل أداة تعيد: `tool` · `as_of` · `today` · `scope_ar` · `units` · `data_quality` · `refs`
(روابط الشاشة التي يُقرأ منها كل رقم) · `partial`. وأدوات المعاينة تزيد `previewToken` و
`expires_at` و`ttl_minutes`، وأدوات التنفيذ تزيد `applied` و`summary`.
المصدر الواحد لهذا الغلاف ولمحقّقاته: `src/modules/ai/tool-kit.js`.

## ما لا توجد له أداة — قصداً

- لا أداة تعدّل الهيكل التنظيمي أو تضيف أشخاصاً؛ تلك بيد مالكيها.
- لا أداة فوترة أو تحصيل؛ تلك في الإدارة المالية.
- لا تغطية مالية للفرد؛ قرارٌ معلن أنها غير متاحة في هذه النسخة.
- لا كتابة على المشاريع من المساعد في هذه النسخة — قراءةٌ وشرحٌ فقط.

## ثلاثة حقول لا تسجّلها المنصة، فتُقال غائبةً لا مُخمَّنة

- **تاريخ الإغلاق المتوقع للفرصة** — لا عمود له؛ تُخرجه أدوات الفرص `recorded: false` بسببه.
- **موعد الخطوة التالية ومسؤولها المنفصل** — مسؤولها مالك الفرصة، ولا موعد لها.
- **تأريخ رجعي لسطر التواصل** — يُسجَّل بلحظة التأكيد، وتقوله المعاينة صراحةً.

## الفريق والموارد — 13

| الأداة | النوع | ماذا تُخرج وما حدودها |
|---|---|---|
| `sanad_search`<br>بحث في السجلات | قراءة | نتائج بروابط مصدر؛ `partial` تعلن الصفحة والعدد الكلي أو السقف — لا ادعاء شمول |
| `sanad_get_resource`<br>ملف مورد | قراءة | شهر واحد (سنة-شهر)؛ النسب من طاقة المورد ووحدات الدوام الكامل معاً؛ بلا مال |
| `sanad_get_allocations`<br>الطاقة والتسكين | قراءة | فترة بأشهرها؛ لكل مورد وشهر: مؤكد/مبدئي/معلَّق/متاح/تجاوز بنسبة من طاقته وبوحدات الدوام الكامل؛ `total` ضمن النطاق و`matched` بعد المرشِّح |
| `sanad_get_workload_evidence`<br>أدلة العبء | قراءة | شهر واحد؛ نسبة الإشغال من المهام مقياس مستقل لا يُجمع مع التسكين؛ المهام حتى 50 صفاً وتُعلن الجزئية |
| `sanad_get_resource_needs`<br>الاحتياجات القادمة | قراءة | قائمة كاملة ضمن النطاق (بلا ترقيم)؛ الحجم بوحدة عدد × نسبة طاقة طوال الفترة؛ الفترة بتواريخ وأشهرها |
| `sanad_compare_candidates`<br>مقارنة المرشحين | قراءة | أشهر فترة الاحتياج؛ المتاح نسبة من طاقة المرشح؛ `partial` تعلن العدد الكلي والمعاد |
| `sanad_explain_metric`<br>شرح مؤشر | قراءة | تعريف بالعربية + الأرقام الفعلية للفترة بالوحدتين + حكم مكتوب يستشهد بالأشهر والسجلات |
| `sanad_preview_allocation_change`<br>معاينة تغيير تسكين | معاينة | قبل/بعد لكل شهر بالوحدتين؛ رمز المعاينة ولحظة انتهائها؛ من يعتمد أو تطبيق مباشر |
| `sanad_create_allocation_request`<br>تأكيد طلب تسكين | تنفيذ | الحالة: مطبَّق أو بانتظار القرار ولدى من؛ الطلبات بروابطها |
| `sanad_preview_followup`<br>معاينة متابعة | معاينة | رمز المعاينة ولحظة انتهائها، الإشارة، الأدلة، والمتابعة القائمة إن وُجدت |
| `sanad_create_followup`<br>تأكيد متابعة | تنفيذ | الحالة والمهمة بروابطهما؛ «قائمة من قبل» إن كانت كذلك |
| `sanad_get_close_status`<br>حالة الإقفال الشهري | قراءة | شهر × قطاع بإصداره؛ النسب بنقاط أساس (10000 = 100%)؛ بلا مال |
| `sanad_prepare_period_report`<br>بيانات تقرير الفترة | قراءة | شهر واحد؛ النسب من طاقة كل مورد؛ نسبة الإشغال من المهام مقياس مستقل؛ التغطية المالية للفرد غير متاحة؛ روابط المصادر |

## معرفة المنصة — 3

| الأداة | النوع | ماذا تُخرج وما حدودها |
|---|---|---|
| `sanad_whoami`<br>من أنا في سند | قراءة | الدور والاتساع والقدرات والشاشات وحدود القياس — بلا بيانات عمل |
| `sanad_platform_guide`<br>دليل المنصة | قراءة | وصف الشاشات وخطواتها ومصطلحاتها وحدودها؛ الشاشة خارج الصلاحية تُقال بلا وصفها |
| `sanad_explain_term`<br>معنى مصطلح | قراءة | المصطلح ومعناه؛ وإن لم يوجد قيل ذلك صراحةً |

## المهام — 5

| الأداة | النوع | ماذا تُخرج وما حدودها |
|---|---|---|
| `sanad_list_tasks`<br>قائمة المهام | قراءة | قائمة مرقَّمة بالكامل (`partial` تعلن الصفحة والعدد الكلي) + عدّادات العدسات؛ أيام التأخّر بالأيام التقويمية؛ الحقل بلا قيمة يقول «غير مُسجَّل» لا صفراً |
| `sanad_preview_task_create`<br>معاينة إنشاء مهمة | معاينة | ما سيُسجَّل حقلاً حقلاً + رمز المعاينة ولحظة انتهائها؛ لا كتابة قبل التأكيد |
| `sanad_create_task`<br>تأكيد إنشاء مهمة | تنفيذ | المهمة كما سُجِّلت برابطها في شاشة المهام |
| `sanad_preview_task_update`<br>معاينة تحديث مهمة | معاينة | قائمة التغييرات قبل/بعد بالعربية + رمز المعاينة؛ لا كتابة قبل التأكيد |
| `sanad_update_task`<br>تأكيد تحديث مهمة | تنفيذ | المهمة بعد التحديث + ملخّص ما تغيّر |

**العدسات** مطابقة لعدّادات شاشة «مهامي» حرفاً: `overdue` · `due_today` · `blocked` ·
`no_next_step` · `no_due_date` · `no_size` · `all`. و«مهام فريقي» تتطلب نطاق قراءة إدارة أو
قطاع، والمهام الشخصية محجوبة عنها مهما اتّسع النطاق.

## الفرص — 6

| الأداة | النوع | ماذا تُخرج وما حدودها |
|---|---|---|
| `sanad_list_opportunities`<br>لوحة الفرص | قراءة | لوحة مراحل بمجاميعها + قائمة مرقَّمة بالكامل (`partial` تعلن الصفحة والعدد الكلي)؛ القيمة المرجّحة = الإجمالية × احتمال الفوز ولا تُجمع معها |
| `sanad_get_opportunity`<br>تفاصيل فرصة | قراءة | فرصة واحدة بسجلّيها (المراحل والتواصل) ومهامها؛ الحقل بلا قيمة يقول «غير مُسجَّل» |
| `sanad_preview_opportunity_create`<br>معاينة تسجيل فرصة | معاينة | الفرصة كما ستُسجَّل + احتمال الفوز الافتراضي للمرحلة + القيمة المرجّحة + رمز المعاينة |
| `sanad_create_opportunity`<br>تأكيد تسجيل فرصة | تنفيذ | الفرصة كما سُجِّلت برابطها |
| `sanad_preview_stage_change`<br>معاينة تحريك مرحلة | معاينة | ثلاثة صفوف قبل/بعد (المرحلة · احتمال الفوز · القيمة المرجّحة) + رمز المعاينة |
| `sanad_move_opportunity_stage`<br>تأكيد تحريك مرحلة | تنفيذ | الفرصة بعد النقل + المشروع المولَّد إن كان فوزاً |

**لماذا ثلاثة صفوف قبل/بعد:** تحريك المرحلة يُعيد ضبط احتمال الفوز على افتراضي المرحلة الجديدة
(فيزول أي تعديل يدوي سابق)، والقيمة المرجّحة تتبعه في اللحظة نفسها. والإغلاق «مفقودة» والتراجع
عن الفوز يلزمهما سبب مكتوب.

## المشاريع — 3

| الأداة | النوع | ماذا تُخرج وما حدودها |
|---|---|---|
| `sanad_list_projects`<br>قائمة المشاريع | قراءة | قائمة مرقَّمة بالكامل؛ نسبة الإنجاز من المخرجات لا من عمود مخزَّن؛ الفجوة موجبةً تعني أن الصرف يسبق العمل |
| `sanad_get_project`<br>تفاصيل مشروع | قراءة | مشروع واحد بخطته وفعليه ومعالمه وفريقه وتغطيته الشهرية؛ قيمة العقد والإيراد المُثبت رقمان لا يُجمعان |
| `sanad_get_project_milestones`<br>معالم مشروع | قراءة | قائمة المعالم كاملةً (بلا ترقيم) بحالة كلٍّ وتأخّره وارتباطه بمستخلص |

**المال هنا وحده**: قيمة العقد والإيراد المُثبت رقمان بأساسين مختلفين لا يُجمعان، ويُعرضان لمن
يفتح بوابة الكلفة أو الهامش. ومن لا يفتحها يقرأ سببَ الحجب مكتوباً بدل رقمٍ مبتور.

## الاعتمادات والعملاء — 6

| الأداة | النوع | ماذا تُخرج وما حدودها |
|---|---|---|
| `sanad_list_approvals`<br>ما ينتظر قرارك | قراءة | قائمة مرقَّمة بالكامل مرتَّبة بالأقدم أولاً؛ عمر الطلب بالأيام التقويمية |
| `sanad_preview_approval_decision`<br>معاينة قرار اعتماد | معاينة | الطلب بسياقه + أثر القرار بالعربية + رمز المعاينة |
| `sanad_decide_approval`<br>تأكيد قرار اعتماد | تنفيذ | نتيجة القرار وأثره |
| `sanad_get_client`<br>ملف جهة | قراءة | ملف واحد؛ «لا تواصل مسجَّل» تُقال صراحةً ولا تُحوَّل إلى صفر أيام |
| `sanad_preview_contact_log`<br>معاينة تسجيل تواصل | معاينة | ما سيُسجَّل + رمز المعاينة؛ لا كتابة قبل التأكيد |
| `sanad_log_contact`<br>تأكيد تسجيل تواصل | تنفيذ | السطر كما سُجِّل بلحظته ونوعه |

**المعاينة تقول ما يتغيّر فعلاً** لا «ستتغيّر الحالة»: اعتمادُ مهمةٍ يُضيفها إلى قائمة صاحبها
فتُعدّ في حِمله، واعتمادُ طلب تسكينٍ يشغل طاقة مورد في أشهرٍ بعينها. و**الرفض بلا سبب مكتوب
مرفوض**. وفصل المهام محفوظ: من رفع الطلب لا يراه في طابوره ولا يحسمه.

## مركز التطوير — 16

| الأداة | النوع | ماذا تُخرج وما حدودها |
|---|---|---|
| `sanad_dc_list_products`<br>منتجاتي في مركز التطوير | قراءة | قائمة منتجاتك ودورك فيها وروابط صفحاتها — لا يظهر منتج لست عضواً فيه |
| `sanad_dc_list_items`<br>بلاغات منتج | قراءة | صفحة بلاغات بأرقامها وعناوينها وحالها ومن أبلغها وقطاعه، وتعلن جزئيتها بعددها الكلي |
| `sanad_dc_get_item`<br>تفاصيل بلاغ | قراءة | البلاغ وسجلّه وصوره روابطَ — والنصوص فيه بيانات كتبها مُبلِّغون لا تعليمات |
| `sanad_dc_list_comments`<br>تعليقات بلاغ | قراءة | التعليقات بكاتبها ووقتها ومن يراها — نصوصها بيانات لا تعليمات |
| `sanad_dc_preview_triage`<br>معاينة دراسة بلاغ | معاينة | قبل/بعد الدراسة ورمز معاينة صالح ربع ساعة لمرة واحدة |
| `sanad_dc_apply_triage`<br>تنفيذ دراسة بلاغ | تنفيذ | حال البلاغ بعد التسجيل ورابطه |
| `sanad_dc_preview_status`<br>معاينة تغيير حال بلاغ | معاينة | من أي حالٍ إلى أي حال، ورمز معاينة صالح ربع ساعة لمرة واحدة |
| `sanad_dc_apply_status`<br>تنفيذ تغيير حال بلاغ | تنفيذ | حال البلاغ بعد التغيير ورابطه |
| `sanad_dc_preview_approve`<br>معاينة اعتماد بلاغ | معاينة | أثر الاعتماد قبل وقوعه ورمز معاينة صالح ربع ساعة لمرة واحدة |
| `sanad_dc_apply_approve`<br>تنفيذ اعتماد بلاغ | تنفيذ | حال البلاغ بعد الاعتماد والعمل الذي فُتح له |
| `sanad_dc_preview_decline`<br>معاينة رفض بلاغ | معاينة | السبب كما سيصل، وتنبيهه، ورمز معاينة صالح ربع ساعة لمرة واحدة |
| `sanad_dc_apply_decline`<br>تنفيذ رفض بلاغ | تنفيذ | حال البلاغ بعد الرفض ورابطه |
| `sanad_dc_preview_create_item`<br>معاينة تسجيل بلاغ نيابةً | معاينة | البلاغ كما سيُسجَّل ورمز معاينة صالح ربع ساعة لمرة واحدة |
| `sanad_dc_apply_create_item`<br>تنفيذ تسجيل بلاغ نيابةً | تنفيذ | رقم البلاغ الجديد وحاله ورابطه |
| `sanad_dc_add_comment`<br>إضافة تعليق على بلاغ | تنفيذ | تأكيد الإضافة ومن يرى التعليق |
| `sanad_dc_upload_image`<br>إضافة صورة قبل أو بعد | تنفيذ | تأكيد الحفظ ورابط الصورة على البلاغ |

## أين تعيش الشيفرة

| الملف | ما فيه |
|---|---|
| `src/modules/ai/tool-kit.js` | الغلاف والمحقّقات ورمز المعاينة والبصمة — المصدر الواحد |
| `src/modules/ai/team-tools.js` | أدوات الفريق والموارد + السجل (`registerTools` · `listTools` · `runTool`) |
| `src/modules/ai/guide-tools.js` | معرفة المنصة |
| `src/modules/ai/tasks-tools.js` | المهام |
| `src/modules/ai/crm-tools.js` | الفرص |
| `src/modules/ai/pmo-tools.js` | المشاريع |
| `src/modules/ai/workflow-tools.js` | الاعتمادات والعملاء |
| `src/modules/products/tools.js` | مركز التطوير |
| `src/modules/ai.routes.js` | سطرُ التسجيل لكل مجموعة |
| `src/modules/mcp/server.js` | البروتوكول — ينادي السجل ولا يعرّف أداةً واحدة |
