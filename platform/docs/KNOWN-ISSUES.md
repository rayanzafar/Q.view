# Known Issues & Deferred Items — سند

Registry of every open defect, deliberate deferral, and pending owner decision. **The code is authoritative**; every row below was re-verified against the working tree on 2026-08-06 (branch `claude/evc-platform-analysis-r5nsri`, 1447 tests / 0 fail, migrations 001–028). Paths are relative to `platform/`. Items already fixed on this branch are not listed.

**KI-051 … KI-067 (المستخلصات) added 2026-08-18** from an end-to-end audit of the progress-claim flow (data → service → API → pages → reports → tests); each finding was adversarially re-verified against the code before filing, and KI-054 / KI-059 were reproduced on a fresh database. Sequence: KI-051–KI-055 (money/lifecycle correctness), KI-056–KI-058 (exposure & data integrity), KI-059–KI-065 (lifecycle, perf, reads, copy, tests), KI-066–KI-067 (owner decisions, see D18).

Severity: **high** = data/money/security exposure or deploy risk today · **med** = real gap, bounded blast radius · **low** = latent/cosmetic/tooling.
Status: **open** (fix when scheduled) · **deferred-by-design** (documented decision, revisit trigger noted) · **partially-addressed** · **owner-decision** (product owner must decide first; not a defect).

---

## Open

| ID | Area | Description | Sev | Status | Evidence | Next action (owner) |
|----|------|-------------|-----|--------|----------|---------------------|
| KI-001 | auth | `must_change_pw` is stored and returned at login (`mustChangePassword`) and cleared by change-password, but **no middleware enforces it** — a user flagged to change their password keeps working forever; `seed-admin.js` writes 0. SECURITY-REPORT still lists it as an applied control. | med | open | `src/modules/auth.routes.js:26`, `src/core/auth/service.js:72,92`, `scripts/seed-admin.js:54` | Add an enforcement middleware + redirect page; regression test (dev) |
| KI-002 | web/XSS | HTML helpers interpolate arguments **without `esc()`**: `statMini`, `noticeCard` (renders `e.message`), `pill`. Safe today because callers pass static/formatted values — latent XSS foot-gun the moment a caller passes user text. | med | open | `src/web/views/_shared.js:35,120`, `src/web/layout.js:543` | Escape at the sink like `layout()` title/subtitle; audit callers for double-escape (dev) |
| KI-003 | ops/migrate | `migrate.js` runs each `.sql` via `exec` with **no wrapping transaction**: on SQLite a mid-file failure leaves partial DDL applied with no `schema_migration` row → re-run collides with half-created objects. (PG multi-statement `exec` is implicitly atomic.) | med | open | `scripts/migrate.js:18-24` | Wrap per-file apply+record in a tx on SQLite (dev) |
| KI-005 | ops/boot | Boot takes ~2 min (sequential seed/backfill one-shots in `boot.sh`) against a 300 s Railway healthcheck timeout — each added boot step eats the margin; a slow boot = failed deploy. | med | open | `scripts/boot.sh`, `railway.json:12` | Don't add boot steps without measuring; consider parallel/skip-fast paths (dev) |
| KI-006 | http | Body-size limits disagree: JSON body 1 mb, io upload raw 15 mb, bare-metal nginx path caps 12 m — a near-15 MB import upload would 413 behind the nginx deploy path. | low | open | `src/server.js:38`, `src/modules/io/io.routes.js:25`, `deploy/staging.nginx.conf:18` | Align nginx to 16m or document the io cap as 12 MB (dev) |
| KI-007 | tests | `src/modules/search/*` (global search, Ctrl+K) has no dedicated tests — only two probes inside `tests/security/exec-surfaces.test.js:211-213`. Scope-inheritance claims are untested directly. | low | open | `src/modules/search/search.js`, `tests/security/exec-surfaces.test.js:211` | Add unit/integration tests for scoping + category drop (dev) |
| KI-008 | sql/portability | Alias-less `FROM (subquery)` in `availableYears()` — legal on SQLite and PG ≥ 16 (staging runs PG 18), **error on PG < 16**. Latent portability trap; the convention "sub-SELECT in FROM needs an alias" is manual only. | low | open | `src/core/reports/metrics.js:28-32` | Add the alias; keep the convention in review checklists (dev) |
| KI-009 | seed | `seed-roles` under demo-seed (`SANAD_SEED_DEMO=1`) writes accounts then crashes in `seedDemoOrg` — unlinks an employee not yet created (`unlinkUserFromEmployee`, org.js:496 → `employeeForLink` throws «الموظف غير موجود أو محذوف»), so demo departments/employees are skipped each boot. Staging unaffected (seed off there). | low | open | `scripts/seed.js:130-132`, `src/modules/org/org.js:444`, `docs/CHANGELOG.md:376` | Guard the unlink for missing employees; test under demo-seed (dev) |
| KI-010 | repo | Repo bloat: `docs/evidence/` holds **128 MB** of screenshot packs in the working tree and git history — slow clones, near GitHub limits. | low | open | `docs/evidence/` (du -sh = 128M) | Stop committing packs; owner to approve history rewrite (dev + product owner) |
| KI-011 | docs/changelog | CHANGELOG version numbering collides: two `v3.9` (lines 358, 486), two `v4.0` (312, 336), no v4.1–v4.3, no v4.5 (v4.4→v4.6). Deploy markers are non-monotonic (v2.8–2.11 «بانتظار نشر», v3.8/v3.9 «لم تُنشَر بعد» below deployed versions) — "in CHANGELOG" ≠ "on staging". | low | open | `docs/CHANGELOG.md:312,336,358,486` | Freeze existing numbers (history), keep future entries monotonic with explicit deploy state (dev) |
| KI-012 | tooling/docs | Stale "17 demo personas" comment — `ROLES` actually lists 16 (matching 16 RBAC roles). | low | open | `scripts/lib/expectations.mjs:14` | Fix the comment (dev) |
| KI-031 | rbac/ops | Retired **`finance` role is live on staging** with **40 company-scope grants** (incl. `invoice`/`expense` **approve** and `margin`/`cost` **read**), held by 0 users. Migration 018 retired it and it is absent from `ROLE_GRANTS`, but `seed-rbac.js` seeds/zeroes only roles **in** the matrix and **never prunes a role that left it**, so the role (re-created 2026-08-01, after 018) lingers with its old grants — the exact hazard 018's preamble warns of. `finance-role-retired.test.js` builds a **fresh** migrate+seed DB, so it cannot catch this live drift. The role is still admin-assignable and carries unredacted margin/cost + invoice/expense approve if assigned. Found by the reference-doc audit (VERIFICATION-REPORT-2026-08-09.md M2). | med | open | live `role`/`role_permission` (finance, created 2026-08-01, deleted_at NULL); `scripts/seed-rbac.js:7-19`; `migrations/018_retire_finance_role.sql`; `tests/security/finance-role-retired.test.js:14-56` | Delete the orphaned live `finance` role+grants (read-only audit did not mutate); make seed-rbac prune matrix-absent system roles; extend the guard test to assert against an existing/seeded DB, not only a fresh one (dev + product owner) |
| KI-032 | workflow | `myApprovalQueue` filters `ar.sector_id = user.sector_id` for non-admins, so a **company-scope approver seeded with a `sector_id`** (e.g. `ceo_office`) never sees an escalation step routed to it from **another sector**. Latent today only because the role-based workflows are unwired (KI-033); becomes blocking the moment a submit path is added. | med | open | `src/modules/workflow/engine.js:201-202` | Treat a company-scope approver (or `approver_scope='company'`) as sector-agnostic in the queue+notify filters; regression test (dev) |
| KI-033 | workflow/governance | The five role-based approval workflows (opportunity/proposal/expense/deliverable/timesheet) are **dormant scaffolding**: `submitForApproval` has **no UI caller** (only raw `POST /api/approvals`), so the seeded amount-threshold escalation (`min_amount_halalas`, e.g. expense ≥ 2M SAR → CEO office) never runs on the path users use. Expenses are approved by a **direct status flip** gated only by `can(user,'approve','expense')` with no threshold. Only task approvals + staffing confirmations (the direct-settler rail) are actually wired. The reference doc (p22) presents the threshold path as the live mechanism. Proposals additionally have **no create/update route** at all despite tables+grants+workflow. | med | open | `src/modules/workflow/engine.js:106-108` (stepFor, correct but unreached), `src/modules/finance/expenses.js:222-226`, `src/modules/api.routes.js:133` (only submit caller), `src/web` (no submit caller) | Owner decides: wire the submit path for these resources, or drop the seeded thresholds and document direct approval as intended (product owner → dev) |
| KI-035 | workflow/rbac | **Residual after v5.12** (the invisible-approval half is fixed: every directed pending request now renders on «صفحتي» with inline approve/reject for any role, plus batched email — verified by `tests/integration/home-approvals.test.js` KI-035 regression). Remaining: (a) `department.manager_user_id` is still settable to any account with no role check, and the org-tree picker offers every active user; acceptable now that any assignee can act from home, but (b) **directed requests are not re-routed when `manager_user_id` changes** — the frozen `assignee_user_id` keeps the old manager as the only decider; and (c) no admin «معلَّقة بلا معتمِد» view. | med | open | `src/modules/org/org.js:195-231`, `src/web/views/org.js:349-351`, `src/modules/workflow/engine.js:277-284` | Re-route (or re-queue) open directed requests when the department manager changes; admin view for stuck requests (dev) |
| KI-036 | rbac/pmo | **«مهامي» shows the name of a project/opportunity the reader cannot open, and links to it — the link 403s.** `myTasks` (`tasks.js:104-113`) `LEFT JOIN`s `project`/`opportunity` for `project_name`/`opportunity_name` with **no scope filter**, so `parentOf` (`views/pmo.js:566-571`) always takes the named branch and its designed out-of-scope fallback «جهة خارج نطاقك» is **unreachable dead code**. Reproduced with two personas: `demo.employee` and a non-demo `employee`-role account each received a task linked to `FX-OPP-2` / `opp_ITTQeBDngUwq`; the row rendered `<a class="tk-parent" href="/app/opportunity/…">دراسة تطوير المشاعر المقدسة</a>` and clicking it returned **403 «هذه الصفحة خارج صلاحياتك»**. Same class of «يُعرَض ولا يُفتح» leak that v5.8/v5.9 closed on the contract, staffing-roster and export surfaces — this one was missed. | med | open | `src/modules/pmo/tasks.js:104-113`, `src/web/views/pmo.js:566-583`, CHANGELOG v5.8/v5.9 (same leak class) | Scope the name joins in `myTasks` (as v5.9 did elsewhere) so out-of-scope parents fall back to «جهة خارج نطاقك» with no href; regression test asserting the fallback fires (dev) |
| KI-037 | notifications | **Residual after v5.13** (the «صفحتي» card + batched email landed in v5.12; the bell now counts live pending approvals via `GET /api/approvals/pending-count` — self-clearing on decision — and links to the home card). Remaining: `notification` rows (task assigned, approval decided, …) still have **no reading surface** — the body is rendered nowhere and nothing calls `POST /api/notifications/:id/read`; they are a write-only record until a notifications list/page exists. | low | open | `src/modules/notifications/notify.js`, `src/modules/api.routes.js` (notifications routes) | A notifications reading surface (list + mark-read) as its own wave (product owner → dev) |
| KI-051 | finance/claims | **Auto-claim re-bills work the legacy system already billed.** The only "already billed" check is `invoice_line` rows (`billedAmong`, `finance.js:343-348`), but migrated legacy invoices link via `invoice.deliverable_id` with **no** `invoice_line` rows (`scripts/migrate-legacy.js:187-191`; `backfill-moep-invoices.mjs` same), and migration 017 rewrote those deliverables back to DELIVERED/ACCEPTED with only an `invoiced_at` stamp — which `createProgressClaim` never reads. On live data (per `progress.js` comment: ~14.9M SAR migrated invoices, 11/342 deliverables stamped) one auto-claim call re-invoices already-billed work. There is also **no over-billing guard** vs `contract.value_halalas` (billed_pct >100% renders raw). Finance UI is owner-disabled (`pages.js:75`) so exposure is API-only today — becomes one-click when re-enabled. Found+adversarially verified 2026-08-18 (claims audit). | high | open | `src/modules/finance/finance.js:343-348,374-377,384`, `scripts/migrate-legacy.js:187-191`, `migrations/017_delivery_money_phases.sql:74-85` | Billed-check must also consult `deliverable.invoiced_at` and `invoice.deliverable_id`; warn/refuse past contract value; regression test with a legacy-shaped fixture (dev) |
| KI-052 | finance/concurrency | **All claim/collection guards run before the transaction, with no DB uniqueness backstop.** `createProgressClaim`: `billedAmong`, eligibility filters and `claimCount` are plain reads before `tx()` (`finance.js:343-387` vs 394) — two concurrent requests double-bill the same deliverable and mint duplicate `claim_no`/`code` (no UNIQUE on `invoice_line.deliverable_id`, `invoice.code`, or `(contract_id, claim_no)` — migration 002 has only plain indexes). `recordCollection`: `outstanding()` check at `finance.js:425-426` precedes the tx — two concurrent full payments both pass → 200% collected, forced PAID, `collectionRate` >100 (over-collection then invisible: `outstanding` clamps at 0). Also `amt > out+1` lets a settled invoice accept unlimited 1-halala payments. Verified 2026-08-18. | med | open | `src/modules/finance/finance.js:343-387,394,418-452`, `migrations/002_progress_claims.sql`, `src/core/db/index.js:97-115` (plain BEGIN/COMMIT) | Re-check billed set + outstanding inside the tx; add UNIQUE partial index on `invoice_line.deliverable_id` and `(contract_id, claim_no)`; concurrency regression tests (dev) |
| KI-053 | finance/lifecycle | **No cancel/void/delete path for any invoice — a mistaken مستخلص is permanent.** Only invoice writers in src are `createProgressClaim` (insert) and `recordCollection` (status flip); nothing writes `CANCELLED` or `invoice.deleted_at`, nothing clears `deliverable.invoiced_at`/`collected_at`, `remove.js` has no invoice/contract kind. Yet `governance.js:165` blocks deleting a billed deliverable with «ألغِ الفاتورة أولاً من صفحة المالية» — a flow that **does not exist** (and the page it names is disabled). `billedAmong` ignores parent-invoice status/deleted_at, so even a manually-cancelled invoice would lock its deliverables forever. Verified 2026-08-18. | med | open | `src/modules/finance/finance.js:346,395,439`, `src/modules/pmo/governance.js:163-166`, `src/core/lifecycle/remove.js` (REMOVABLE registry) | Owner defines void policy (who/when); then a void service that releases deliverables + excludes voided invoices from `billedAmong` and totals; fix the dead-end message (product owner → dev) |
| KI-054 | finance/claims | **Unpriced deliverables are silently billed at 0 ر.س. and permanently consumed.** NULL prices are deliberate («القيمة غير المتفق عليها تُخزَّن فراغاً لا صفراً», `governance.js:53-58`), but the auto path selects them with no amount filter, coerces NULL→0, writes a 0-amount `invoice_line`, stamps `invoiced_at` — and `billedAmong` excludes them from every future claim (no un-bill path, KI-053). **Reproduced empirically 2026-08-18** on a fresh DB: contract with two NULL-priced delivered deliverables → 0-halala ISSUED invoice; after pricing, both claim paths permanently refuse; `recordCollection` rejects any payment against the 0-invoice. **Widened by v5.32 (on `origin`, not yet in this working tree):** `attachRevenueLine` deliberately creates and then later prices **valueless DELIVERED/ACCEPTED** deliverables («اختر مخرَجاً بلا قيمة») — precisely the population this trap consumes, and pricing-after-delivery is now a sanctioned flow rather than an accident. | med | open | `src/modules/finance/finance.js:374-379,384,406,410`; origin `0286bc0` `src/modules/pmo/governance.js` (`attachRevenueLine`) | Exclude NULL/0-priced deliverables from the auto path (name them in the explicit-path rejection); refuse issuing a 0-amount claim; regression test (dev) |
| KI-055 | pmo/finance | **A billed deliverable stays fully mutable, so PMO and finance report contradictory billed numbers.** `patchRow` guards only delete + direct stamp writes; `amount_sar` on an `invoiced_at`-stamped deliverable is freely editable — PMO's invoiced/collected figures sum **current** deliverable amounts (`progress.js:99-100`, rendered `pmo.js:2084-2085`) while finance sums the frozen `invoice.amount_halalas` → edit 300k→500k and the two surfaces diverge with no audit link. A status rollback (ACCEPTED→REJECTED) also deletes the recognized `revenue_line` while the issued invoice stands. Verified 2026-08-18. **New writer in v5.32 (on `origin`, not yet in this working tree):** `attachRevenueLine` reprices a deliverable through `updateItem` and guards only on `amount_halalas > 0` — it never checks `invoiced_at`, so a deliverable already burned at 0 by an auto-claim (KI-054) passes the guard and gets a value while its ISSUED invoice line stays frozen at 0. | med | open | `src/modules/pmo/governance.js:159-166,190`, `src/modules/pmo/progress.js:99-100`, `src/modules/finance/recognition.js:56-65`; origin `0286bc0` (`attachRevenueLine`, no `invoiced_at` guard) | Block or explicitly warn+audit repricing/regressing a billed deliverable; decide which number is authoritative and reconcile the two surfaces; regression test (dev) |
| KI-056 | web/XSS | **Latent stored XSS in the contract page invoice rows**: `i.period_label` and `i.code \|\| i.id` interpolated without `esc()` (`views/finance.js:116`), and `periodLabel` is stored verbatim from an unvalidated body (`finance.js:401`, route `api.routes.js:169`). Any invoice-create grant holder can persist hostile markup **today** via POST `/api/finance/progress-claim`; it fires for every viewer the moment finance pages are re-enabled (currently `PAGE_ACCESS.finance = () => false`). CSP is Report-Only + `unsafe-inline` (KI-013), so no mitigation at the sink. Same class as KI-002. | med | open | `src/web/views/finance.js:116`, `src/modules/finance/finance.js:401`, `src/modules/api.routes.js:169`, `src/core/http/security.js:30` | `esc()` at the sink; length-cap/validate `periodLabel` at the service; XSS regression test for finance views (dev) |
| KI-057 | finance/reads | **Collected-side queries skip the filters their invoiced-side twins apply** — the same asymmetry class `financeByPM` already fixed (comment `finance.js:182-183`): (1) `financeSummary` collected (`finance.js:106-109`) lacks `i.deleted_at IS NULL` + DRAFT exclusion → `collectionRate` can exceed 100%; (2) `financeByContract` per-contract collected (`finance.js:221`) same; (3) the «فواتير غير مرتبطة بعقد» bucket's collected (`finance.js:238-239`) additionally drops the caller's **scope clause** its invoiced twin applies → a sector-scoped caller of GET `/api/finance/by-contract` receives company-wide collection sums (cross-sector cash leak; API-only today, finance page disabled). Verified 2026-08-18. | med | open | `src/modules/finance/finance.js:105-109,219-221,233-244` | Mirror deleted_at/DRAFT/scope filters on every collected side (extend the KI-fix pattern of financeByPM); scoped-role regression test (dev) |
| KI-058 | io/revenues | **Revenues import round-trip silently corrupts net-revenue reporting.** `applyRow`'s update (and `undoRow`) write `amount_halalas` only; rows with a stored `net_amount_halalas` (all post-019 rows) keep the **stale net** under `netSql`'s COALESCE — an imported gross change updates the gross but never reaches net-revenue KPIs (metrics NET_REVENUE, project-cash, exec, sector). And `resolveRow` matches bare ids, so imports freely edit `rl_dlv_*` auto rows that `recognition.js` derives from deliverables — the next deliverable save overwrites the import. Verified 2026-08-18. | med | open | `src/modules/io/adapters/revenues.js:97` (+undoRow), `src/modules/finance/vat.js:73-93`, `src/modules/finance/recognition.js:33,52-94` | Recompute net/vat via `splitGross` on import update/undo (or null the stored net); refuse or flag edits to `auto=1` rows; round-trip regression test (dev) |
| KI-059 | lifecycle | **`remove.js` collection blocker is dead code**: the money guard lists `{ table: 'collection', col: 'project_id' }` but `collection` has never had `project_id` (001 DDL; 019 adds only net/vat) — `shapeOf` probes, misses, and `countLive` silently returns 0, the exact failure mode the file's own comment warns about. Today the invoice blocker incidentally shields collections, but it counts only `deleted_at IS NULL` invoices. 3 of the 4 money-blocker rows are untested. Reproduced 2026-08-18 (orphaned-collection scenario). | med | open | `src/core/lifecycle/remove.js:104-109,221-229`, `migrations/001_init.sql:645-652` | Count collections via a join through `invoice_id`; test all four money-blocker rows (dev) |
| KI-060 | perf/finance | **N+1 query storms in the finance aggregates**: `outstanding()` is one SELECT per invoice, and `financeSummary` awaits it in a loop, then `dso()` and `arAging()` each re-load and loop again — ~3N sequential queries per summary render for N open invoices in the year; `financeByContract` issues 2 queries per contract (≤200 contracts, concurrent but pool-capped at 10). Grows with open-invoice count exactly where billing history accumulates; each query is a network round-trip on live PG. Verified 2026-08-18. | med | open | `src/modules/finance/finance.js:58-61,110-121,132-153,216-221` | Replace per-row `outstanding()` with one GROUP BY collections join reused by summary/aging/DSO; before finance pages re-enable (dev) |
| KI-061 | finance/reads | **Invoices with NULL `issue_date` vanish from every year at once**: `YEAR_PRED` (`finance.js:39`) is a CAST on `substr(issue_date,1,4)` with no NULL branch → the row fails the predicate for all years in invoiced totals AND AR/aging/DSO — a receivable that exists nowhere. `createProgressClaim` always sets the date, but `migrate-legacy.js:189` inserts `issue_date: deliveredAt \|\| null`; live rows depend on the one-off backfill having caught every case. Verified 2026-08-18. | low | open | `src/modules/finance/finance.js:39,105-110,132,151,181`, `scripts/migrate-legacy.js:189` | NULL-date branch (bucket as «بلا تاريخ» like `invoicedByMonth` does) or assert-and-backfill on live; test (dev) |
| KI-062 | finance/claims | **Claim metadata edges**: (1) `contractDetail` orders invoices by `issue_date, claim_no` but `claim_no` is TEXT and unpadded — claim «10» sorts before «2» on a same-day tie (≥10 claims on one contract). (2) `invoice.progress_pct` is write-only (read nowhere), unclamped (>100 storable), and measured **project-wide** against one contract's value — structurally wrong on multi-contract projects. Verified 2026-08-18. | low | open | `src/modules/finance/finance.js:296,391,401-402`, `migrations/002_progress_claims.sql:4` | Order by CAST(claim_no AS INTEGER) (or zero-pad); clamp `progress_pct`, scope it to the contract's own deliverables, or drop the column (dev) |
| KI-063 | ui/copy | **Arabic copy defects in the claim flow**: number agreement wrong for 1–10 («${n} مخرَجاً غير موجود», `finance.js:367-368`; «+ مستخلص من ${n} مخرج مسلّم», `views/finance.js:142`) — the platform's own `countAr` helper is unused here though used with مخرَج elsewhere (`attention.js`, `pmo.js`); unpriced deliverables print `fmtSar(null)` → «0 ر.س.» on the contract table (`views/finance.js:128`), violating «الصفر ليس غياباً»; claim issuance + collection use browser `prompt()` with OS-language buttons instead of the design-system modal, and `views/finance.js` still uses inline `onclick` (KI-013 class). Verified 2026-08-18. | low | open | `src/modules/finance/finance.js:367-368`, `src/web/views/finance.js:120,128,142`, `src/web/public/app.js:80-85`, `src/core/i18n/plural.js` | `countAr` in both strings; «—» for null amounts; replace prompt() with a proper modal when the finance UI wave runs (dev) |
| KI-064 | finance/api | **Silent truncation in finance listings**: `financeByContract` LIMIT 200, `financeByClient` LIMIT 40 — no offset/cursor, no truncated flag in the payload; contract #201 (lowest-value — where forgotten unbilled contracts live) never appears in the API response or any UI. Violates «الغياب ليس صفراً». Verified 2026-08-18. | low | open | `src/modules/finance/finance.js:212,259`, `src/modules/api.routes.js:167` | Paginate or return a truncation flag + total count (dev) |
| KI-065 | tests/finance | **Claim-lifecycle test holes** (beyond each row's own regression test): the HTTP route POST `/api/finance/progress-claim` has zero request-level tests (service is well covered by `progress-claim-scope.test.js`); untested: `recordCollection` validation rejections, claim numbering/code format, `period_label`/`due_date`/`owner_user_id` fields, concurrency (KI-052), XSS on finance views (KI-056), zero-priced auto-claims (KI-054), over-billing (KI-051). Catalogued 2026-08-18. | low | open | `tests/integration/progress-claim-scope.test.js`, `src/modules/api.routes.js:169-170` | Add an HTTP-level claim/collection suite; fold in each KI's regression test as fixed (dev) |
| KI-070 | ops/deploy | `deploy.mjs` auto-discovery of the extracted PostgreSQL 18 client looks one level too shallow: it scans `/tmp/claude-*/<project>/scratchpad/pg18/…` but the real layout is `/tmp/claude-*/<project>/<session-id>/scratchpad/pg18/…`, so with no system `pg_dump` the mandatory backup step fails («pg_dump غير متاح») and the deploy aborts before deploying. Workaround used 2026-08-23: `PG_BIN=<…/pg18/extract/usr/lib/postgresql/18/bin>` (the script then derives `LD_LIBRARY_PATH` correctly). | low | open | `scripts/deploy.mjs:92-104` | Walk one more directory level (session id) in the candidate scan, or document `PG_BIN` in `DEPLOY-PIPELINE.md` (dev) |
| KI-071 | web/foundation | `src/web/public/styles.css` loads AFTER the inline design-system `<style>` and overrides it with contradictory rules (`body{line-height:1.55}` vs the system 1.7; a purple `::selection` vs brand blue). Effectively dead weight that fights the system. | low | open | `src/web/public/styles.css`, `src/web/layout.js` (STYLE + link order) | Delete the file or fold its two rules into `layout.js` (dev) |
| KI-072 | web/perf | Tailwind is shipped as the 407 KB CDN runtime (`src/web/public/tailwind.js`) on every page for ~90 class attributes total (mostly `govern.js`, `people.js`, `crm.js`); its config also declares `muted`/`faint` at different values than the CSS variables of the same names. | low | open | `src/web/layout.js` (script tag + config), `git grep -c "class=\\"[^\\"]*grid "` | Rewrite the ~90 usages with design-system classes and drop the runtime (dev) |
| KI-073 | web/govern+finance | **Unnamed per-row form controls let the browser restore a value onto the wrong row after `location.reload()`.** Controls with no `id`/`name`/`autocomplete` are restored **positionally** (Blink: one `name=""`+type deque consumed in document order; Gecko: a content-index state key), so any re-render that shifts row order slides the previous row's value onto its neighbour. Fixed on the tasks page in v5.40 (stable `id`/`name` per control + a load-time reconcile against `data-status`); **two instances remain**: (1) the project risk register — `views/pmo.js:2650` status select, server order `ORDER BY (status='CLOSED')` (`modules/pmo/governance.js:291`) with a reload at `public/pages/project-governance.js:58`, so closing a risk shifts every later select and all three share the same `OPEN`/`MITIGATING`/`CLOSED` domain (a slid value always matches a real option, so it is silently plausible); (2) the expense ledger — `views/pmo.js:1765,1777`, where settling an expense deletes its inline edit row (`editable = canEdit && !settled`) so the shift is count-driven. Not reproducible in the sandbox: headless Chromium under Playwright performs no form-state restoration at all (verified with an isolated probe), and no Gecko build is installed. | low | open | `src/web/views/pmo.js:1765,1777,2650`, `src/modules/pmo/governance.js:291`, `src/web/public/pages/project-governance.js:58`, `src/web/public/pages/project-money.js:104` | Apply the v5.40 pattern — `id`/`name` derived from the row id plus `autocomplete="off"` — to those selects, with a markup guard like `tests/integration/tasks-workcentre.test.js` (dev) |
| KI-074 | reports/session | `sessionlessUser()` inserts a real `session` row per scheduled report **per recipient** just to build a scope, with a 60-second `expires_at` and the comment «cleaned by TTL» — nothing cleaned them until v5.42 added the hourly purge (10 such rows were live on staging on 2026-08-25). The purge now collects them, but the row should never be written: a user's scope is derivable without a session row, and a report run leaving login-shaped rows in the session table muddies both the admin session list and any future session forensics. | low | open | `src/core/reports/engine.js:326-333`, `src/core/auth/service.js` (purgeExpiredSessions) | Build the scope directly from the user id (extract the body of `resolveUserFromSession` to take a user row) and stop writing the row (dev) |
| KI-077 | web/pmo | **The author's pending-approval tasks vanish in board and calendar views.** `pendingMine` is stripped from `flat` (`pmo.js:507-511`) and re-inserted only inside `listBody` (`pmo.js:853`) — switch view and freshly-created pending work disappears without trace. Any fix brushes the v5.17/KI-041 owner ruling (the uncounted section excluded «من كل عدّاد ولوح وتقويم»); proposed reading: keep it uncounted but render the same section below all three views. | med | open | `src/web/views/pmo.js:507-511,853,1449` | Plan 03 (T-03); owner decision flagged in `docs/plans/2026-08-25-owner-notes/00-موجز-تنفيذي.md` (product owner → dev) |
| KI-078 | web/pmo | **The DONE column silently shows only the last 14 days.** `doneSince = addDays(today,-14)` (`pmo.js:495` → SQL `src/modules/pmo/tasks.js:86`) truncates the board's «منجزة» column with no visible note — the count is neither the total nor an explained subset. | low | open | `src/web/views/pmo.js:495`, `src/modules/pmo/tasks.js:86` | Plan 03 (T-04): «آخر ١٤ يوماً» sub-label on the column head (dev) |
| KI-079 | pmo/workflow | **Live approval bypass: edits never re-evaluate approval.** `updateTask` (`src/modules/pmo/tasks.js:505-572`) never calls `taskApproval` — create an unlinked task (instant, no approval under today's rule) then PATCH `project_id` onto it: `assertMayLink` runs but no approval is raised and `approval_state` stays NULL. `bulkUpdateTasks` allows the same for up to 100 tasks per call (`tasks.js:489-503`). Bypasses the CURRENT conditional rule, not just the planned universal one. | med | open | `src/modules/pmo/tasks.js:505-572,489-503`, `src/modules/pmo/task-approval.js:72` | Plan: `docs/plans/2026-08-25-owner-notes/01-اعتماد-المهام-الشامل.md` — re-evaluate approval on parent change, charged to the editor (dev) |
| KI-081 | web/pmo | The four focus counters hardcode `view:'list'` in their hrefs (`pmo.js:741-744`), so clicking «متأخرة» from the board silently drops the user out of the board — although all four filters work in board and calendar. | low | open | `src/web/views/pmo.js:741-744` | Plan 03 (T-05): drop the hardcoded view — `qp()` already preserves it (dev) |
| KI-082 | web/pmo | The `assignee` filter half-applies: it filters the list/board but not the person-card board above it (`teamWorkload` takes no assignee), so the page claims «تعرض مهام شخص واحد» (`pmo.js:1448`) while rendering every colleague's card (`pmo.js:954`). | low | open | `src/web/views/pmo.js:954,992,1448` | Plan 03 (T-07): suppress the person-card board when an assignee filter is active (dev) |
| KI-083 | web/pmo | The «لاحقاً» link in the beyond-strip resolves to `win=all` (`pmo.js:864`) because `TASK_WINDOWS` has no `later` value (`pmo.js:432`) — the control does not do what its label says; `laterBand` is computed and rendered but unreachable as a window. | low | open | `src/web/views/pmo.js:432,523,864` | Plan 03 (T-10): add the `later` window + chip (dev) |
| KI-084 | pmo | **Orphan work is unreachable at department scope while the orphan strip points at it.** `teamTasks` requires `emp.department_id IN (…)` (`src/modules/pmo/tasks.js:441-447`), excluding unassigned/inactive-account tasks from the team list and board, yet the strip above instructs «أسنِدها من القائمة أدناه» and links to that very list (`pmo.js:1030-1031`) — an unfulfillable instruction. | med | open | `src/modules/pmo/tasks.js:441-447,795-836`, `src/web/views/pmo.js:1030-1031` | Plan 03 (T-11): OR-branch on `task.department_id` for assignee-less rows, keeping `approvedTaskSql`; scope security test (dev) |
| KI-085 | web/pmo | Active `flag`/`q` filters are invisible: `activeFilters` counts only status/priority/kind (`pmo.js:771`), so arriving via the «مُعطَّلة»/«بلا خطوة تالية» counters leaves the filter block collapsed with no badge, and the only «مسح المرشحات» control is buried inside it (`pmo.js:781`). `flag=noparent` is implemented in SQL (`tasks.js:72`) with no UI entry point anywhere. | low | open | `src/web/views/pmo.js:771-781`, `src/modules/pmo/tasks.js:72` | Plan 03 (T-08/T-09): count flag+q, add flag chips incl. «بلا جهة مرتبطة» (dev) |
| KI-086 | web/crm | **Classification chip counts lie under a year filter, and chips filter in memory over a capped read.** `solCount` is computed from pre-year `allRows` (`src/web/views/crm.js:312-315`) while the board renders post-year, post-filter `rows` (`crm.js:113-115`) — counts mismatch whenever a year chip is active. And the chip filter runs in memory over `listOpportunities`' `LIMIT 500` (`src/modules/crm/opportunities.js:87`) — beyond ~500 rows a chip can silently omit matches. The planned «مصدر المبادرة» chip (plan 04) copies both defects unless fixed with it. | low | open | `src/web/views/crm.js:111-115,312-315`, `src/modules/crm/opportunities.js:87` | Plan 04 (O2): compute counts from `yearRows` now; move both filters into SQL later (dev) |
| KI-087 | ops/env | **`.env.example` has no `SMTP_FALLBACK_*` block**, so the only written record of the fallback channel's variable names is `docs/guides/MAIL-CHANNELS.md`. An operator setting up a new environment from the template gets a platform with no mail fallback and no hint that one exists. Related: `.env.example:32` still ships `MAIL_FROM=Sanad Platform <no-reply@evc.com.sa>` — the exact domain `config.js` documents at length as **live and owned by someone else**, and the reason the code default was removed. Copying the template re-introduces it. | low | open | `platform/.env.example:28,32`, `docs/guides/MAIL-CHANNELS.md`, `src/core/config.js` (smtpFallback block) | Add the `SMTP_FALLBACK_*` block with a format hint, and replace the `MAIL_FROM` example with a placeholder that cannot be a real domain (dev) |
| KI-088 | tooling/sweep | **`scripts/sweep.mjs` carries a dead warning that makes the tool look less trustworthy than it is.** The 'QH-1 known-gap' note claims `/api/org/roster` serves raw `salary_halalas` to roles without the grant. That leak is **closed** — `src/modules/org/org.js:718` gates it behind `can(user,'read','salary')`, and `tests/security/permissions-matrix.test.js:152-178` asserts salary reaches admin only over real HTTP, in both the payload and the rendered page. The warning prints on every deploy sweep. | low | open | `scripts/sweep.mjs:58-63`, `src/modules/org/org.js:718`, `tests/security/permissions-matrix.test.js:152-178` | Delete the stale warning; a sweep that cries wolf trains people to skim it (dev) |
| KI-089 | tooling/deploy | **`scripts/deploy.mjs` step ٥/٧ cannot pass: the stamp it waits for never ships.** `d1ccb3d` (v5.58, committed 2026-08-28 18:25 — *after* that day's 18:15 deploy, so first exercised on 2026-08-29) writes `.build-id` into `platform/` and polls `/ready` for it, but `.build-id` is in `platform/.gitignore:22` and the upload honoured that, so the container reads `null`. On v5.59 the deployment (`a32835c0`) was **SUCCESS** with migration 039 applied and live assets byte-identical, yet the script reported failure and **skipped its own sweep and evidence steps** (run by hand). Until fixed, a red step ٥ after a green step ٤ means «verify by hand», not «roll back». | medium | open | `scripts/deploy.mjs` (BUILD_ID → `.build-id`, poll loop), `platform/.gitignore:22`, `src/core/http/build-id.js:19`, `railway deployment list` 2026-08-29 (a32835c0 SUCCESS), `/ready` → `{"ready":true,"build":null}` | Un-ignore the stamp for the upload (`!.build-id` in `.railwayignore`/`.dockerignore`, or write it to a non-ignored path) and add a gate test that the stamp survives the upload's file selection (dev) |

## Deferred by design

| ID | Area | Description | Sev | Status | Evidence | Next action (owner) |
|----|------|-------------|-----|--------|----------|---------------------|
| KI-013 | web/security | CSP ships **Report-Only**; enforcement blocked by inline `on*` handlers still in server-rendered HTML and `app.js` (49 attributes in `src/web/views`+`layout.js` — 34 onclick + drag/keydown/change; 26 in `src/web/public/app.js` — 22 onclick, e.g. app.js:370-378). New code already uses `data-action` delegation. The in-browser card reader's directives (`'wasm-unsafe-eval'` on `script-src` + `worker-src 'self'`, E2 of «الفعاليات», ADR-0014) are already in the header, so the enforcing flip will not silently break the events capture page. | med | deferred-by-design | `src/core/http/security.js:17-18,33`, `src/web/public/app.js:370` | Migrate remaining `onclick`→`data-action`, then flip the header to enforcing (dev) |
| KI-014 | web/security | CSRF token enforced **only on urlencoded form posts** (`/auth/login-web` exempt); JSON API mutations rely on the `SameSite=Lax` session cookie as the residual defense. | med | deferred-by-design | `src/core/http/csrf.js:15-19` | Extend double-submit check to JSON mutations (dev) |
| KI-015 | rbac/pmo | Projects still carry the list-vs-row scope contradiction: portfolio **list** is built sector-wide (department scope falls open to sector) while row-level read is department-checked. **v5.2 resolved the opportunity half the correct way** — lists narrowed via `deptCol` and the `SECTOR_WIDE_LISTS` row-widener removed — so the mechanism is proven; projects wait only on attribution (see KI-025/D15: narrowing today hides every dept-less project). | med | deferred-by-design | `src/core/rbac/scope.js:65-80`, `src/core/rbac/index.js:95-110`, `docs/adr/ADR-0005-opportunity-visibility.md` | After dept attribution: pass `deptCol` in portfolio queries + regression test (dev, gated on product owner) |
| KI-016 | scale | Single-replica assumptions: report scheduler is an in-process `setInterval`, rate limiter is an in-memory Map, mail account-allowlist is a per-process 60 s snapshot. At >1 replica: duplicate scheduled emails, per-replica limits, stale allowlists. | low | deferred-by-design | `src/core/jobs/scheduler.js:11`, `src/core/http/security.js:37-41`, `src/core/mail/accounts.js:32` | Before scaling >1 replica: DB-lease the scheduler, shared limiter store (dev) |
| KI-017 | db/tls | `PGSSL=require` sets `ssl: { rejectUnauthorized: false }` — TLS without certificate verification (Railway-internal networking workaround). | low | deferred-by-design | `src/core/db/index.js:36` | Revisit if DB leaves Railway's private network; pin CA then (dev) |
| KI-018 | server | `unhandledRejection` logs and **continues** (process left running after an unknown-state rejection); `uncaughtException` exits. Deliberate: don't kill the server for a stray rejection. | low | deferred-by-design | `src/server.js:73` | Keep; watch logs for `!! unhandledRejection` after deploys (dev) |
| KI-019 | server | Graceful shutdown calls `server.close()` but never destroys idle keep-alive sockets — shutdown can stall until the 10 s force-exit timer fires. Bounded, so accepted. | low | deferred-by-design | `src/server.js:92-93` | Optional: track connections and destroy on shutdown (dev) |
| KI-020 | docs/specs | Specs 01/02/03/05/06 are **declared reference-only** with known divergences (e.g. `*_sar NUMERIC` vs implemented `*_halalas INTEGER`); spec 04-email-templates was never produced — templates live in `src/core/mail/templates.js`. On any conflict: code + ADRs win. | low | deferred-by-design | `docs/specs/README.md` | None — do not "fix" specs to match code; extend `07-contracts-delivery2.md` only (dev) |
| KI-034 | rbac/expectation | **إسحاق سيد opens «الفرص» and sees nothing, with no add button — by design, and it surprised him.** Verified live 2026-08-10 (read-only): his account `isaac.sayid@evc.sa` (`usr_P9AsLNqwXxwd`) is healthy — active, two-way linked to employee `u_ishaq_sayed` (إدارة الذكاء الاصطناعي والبيانات / SOLUTIONS); the old `u_ishaq_sayed` duplicate account was already soft-deleted 2026-08-01, so the v3.8 duplicate-pair issue is **not** the cause (an earlier version of this row assumed it was). Cause: his role is **استشاري (consultant)** — opportunity grants are read-own only, no create (`matrix.js:288-296`), and he owns 0 opportunities and has 0 opportunity staffings, so fail-closed scoping correctly yields an empty list and no add button. **Owner decision 2026-08-10: leave as-is** — استشاري seeing an empty «الفرص» is intended; if he later moves into BD work, revisit (role `bd_manager`, or staff him on opportunities via membership). | low | deferred-by-design | live `app_user`/`employee`/`opportunity` (read-only inspection 2026-08-10), `src/core/rbac/matrix.js:288-296`, `src/core/rbac/scope.js:111-148`, `src/web/views/crm.js:149` | None now — explain to إسحاق; revisit only if his job changes (product owner) |

## Partially addressed

| ID | Area | Description | Sev | Status | Evidence | Next action (owner) |
|----|------|-------------|-----|--------|----------|---------------------|
| KI-021 | ops/backup | Durable Postgres backups: `pg-backup.sh` dumps to container-local `data/backups/` (ephemeral — lost with the container). The Railway project now has a **Postgres-PITR bucket**, which partially addresses this; PITR coverage/retention is not yet confirmed and no offsite/restore drill has run against it. | high | partially-addressed | `scripts/pg-backup.sh:6` | Owner to confirm PITR coverage + retention, then run a restore drill (`docs/guides/BACKUP-RESTORE-DRILL.md`) (product owner) |
| KI-022 | docs/adr | ADR-0003 says SQLite is the only supported driver and PG a later step — stale since Postgres went live on staging (dual driver in `src/core/db/index.js`, `DATABASE_URL` switch). Superseding **ADR-0004 is added in this same commit**. | low | partially-addressed | `docs/adr/ADR-0003-database-driver.md:11-12` | Verify ADR-0004 landed and ADR-0003 carries a superseded pointer (dev) |
| KI-023 | docs/security | SECURITY-REPORT.md lags implemented state (e.g. lists `must_change_pw` as an applied control though unenforced — KI-001; CSRF residual described stale vs `csrf.js`). **Being refreshed in this same commit.** | low | partially-addressed | `docs/SECURITY-REPORT.md:12` | Verify the refreshed report matches code; keep it updated per security-relevant commit (dev) |
| KI-024 | docs/readme | README still describes the "vertical slice" era and understates current scope (21 pages / 16 roles / PG staging). **Being rewritten in this same commit.** | low | partially-addressed | `README.md:5` | Verify the rewrite landed (dev) |

## Owner decisions pending (not defects — do not "fix" unilaterally)

| ID | Area | Description | Sev | Status | Evidence | Next action (owner) |
|----|------|-------------|-----|--------|----------|---------------------|
| KI-025 | rbac (D15) | Department scope in **project lists** deliberately fails open to sector: no project is department-attributed yet, so narrowing now would show department managers nothing («استبدال تسريبٍ بعُطل»). **The OPPORTUNITY half is DONE in v5.2 (2026-08-06, owner decision)**: attribution shipped in v5.1, then the visibility flip landed — bd_manager sees own opportunities, department managers see their departments' (primary + partner, read-only), `deptCol` narrowing is live and fails closed on empty sets (ADR-0005). **v5.5 (2026-08-08)**: after a real complaint from the tool owner, corrected the orphan case — opportunities with **no department** now list for department managers in their sector (matching the row-open behavior they already had), so the historical won portfolio (27 of 40 won opps are unattributed) is no longer invisible. Projects remain deferred until their own attribution; blocks KI-015. | med | owner-decision | `docs/OPEN-DECISIONS.md:21`, `src/core/rbac/scope.js:65-80`, `docs/adr/ADR-0005-opportunity-visibility.md`, `tests/security/opportunity-visibility.test.js` | Projects: owner triggers attribution, then dev passes `deptCol` in portfolio queries (product owner → dev) |
| KI-026 | org (D16) | No effective-dating of org-structure changes — moving a department rewrites months of historic reports retroactively. Standard (Workday/SuccessFactors) is effective-dated changes; deferred as a separate wave after the structure stabilizes. Interim containment: audit log keeps every change with its date. | med | owner-decision | `docs/OPEN-DECISIONS.md:22` | Owner decides timing of the effective-dating wave (product owner) |
| KI-027 | org (D17) | Employees cannot see their own place in the org tree (tree view requires an admin-side grant) — «أين أنا ومن مديري» is hidden as a side effect, not a decision; `employee.line_manager_id` exists since 001 with no UI reading/writing it. One permission branch would open it. | low | owner-decision | `docs/OPEN-DECISIONS.md:23` | Owner approves employee self-view; then a single permission-branch change (product owner → dev) |
| KI-066 | finance/claims (D18) | **The claim model cannot express a percentage claim, an advance payment (دفعة مقدمة), retention (محتجز), or non-30-day payment terms.** `createProgressClaim` is the **only** invoice-creation path in the platform and requires DELIVERED/ACCEPTED deliverables billed whole, once — so «٢٠٪ عند التوقيع» is unbillable, a milestone % can only be approximated by pre-pricing deliverables, `retention_halalas` is permanently 0 though every outstanding formula subtracts it (and `pmo.js:1537` teaches users the formula), and `due_date` is hardcoded issue+30 (60/90-day government clients ⇒ every claim falsely flagged «متأخر» by the attention feed and the sector late-claim card). Also unreconciled: nothing checks that Σ deliverable prices equals `contract.value_halalas`, so `billed_pct` can permanently undershoot or exceed 100%; and deliverable prices are treated as **VAT-inclusive** (`finance.js:380-381`) while the entry field is labeled only «القيمة» with no «مع الضريبة» hint — a PM entering net under-bills by ~13%. The deliverable-based model is faithful to the legacy platform, so this is a product decision, **not** a defect to fix unilaterally. Raised 2026-08-18 (claims audit). | med | owner-decision | `docs/OPEN-DECISIONS.md` D18, `src/modules/finance/finance.js:332,380-385,400`, `migrations/002_progress_claims.sql` (`retention_halalas`), `src/web/views/pmo.js:1537,2371`, `src/core/reports/attention.js:43` | Owner decides D18: add an invoice path independent of deliverables (advance/percentage/retention + per-contract payment terms), or ratify the deliverables-only model and remove retention from the formulas and UI. Blocks re-enabling the finance page (product owner → dev) |
| KI-067 | finance/ui | **The claim/collection UI is disabled platform-wide and, when re-enabled, issues an irreversible invoice from a browser `prompt()`.** `PAGE_ACCESS.finance = () => false` (owner decision, pinned by `project-money-visibility.test.js:70`) blocks `/app/finance` and, by inheritance, `/app/contract/:id` — the only page carrying «+ مستخلص» and «تسجيل تحصيل». Meanwhile POST `/api/finance/progress-claim` and `/api/finance/collections` stay live for any grant holder, so claims are issuable **only** by raw API today and with no HTTP-level test (KI-065). When the page returns: `Sanad.progressClaim` prompts only for a period label and claims **every** eligible deliverable at once — the service's selective `deliverableIds` path (with its excellent validation) has no UI caller — the invoice is born `ISSUED` with no draft/preview and no cancel (KI-053). | med | owner-decision | `src/core/policy/pages.js:75`, `src/web/nav.js:14`, `src/web/views/finance.js:142`, `src/web/public/app.js:80-85`, `src/modules/api.routes.js:169-170` | Owner decides when finance returns; that wave must ship deliverable selection + pre-commit preview (use the existing `deliverableIds` path) and resolve KI-051…KI-057 first (product owner → dev) |
| KI-028 | ops/staging | 9 of 16 demo role accounts can't log in on staging (`SANAD_SEED_DEMO=0` → seed skips them), so live sweeps exercise 7 roles; the other 9 are covered by `tests/security/permissions-matrix.test.js` only. Reactivating means demo accounts on a production-like environment. | low | owner-decision | `scripts/seed-staging.js:19-20`, `docs/SESSION-HANDOFF.md:125` | Owner decides: accept 7-role live sweeps, or provision throwaway accounts for full sweeps (product owner) |

---

## How to use this file

- **New defect found** → add a row immediately (next free `KI-###`, never reuse an ID), with severity, status, `file:line` evidence, and a named next action + owner.
- **Fixing a row** → remove it **in the same commit** as the fix, add a regression test, and record it in `docs/CHANGELOG.md`. A fix without test + changelog entry is not done.
- **Status changes** (e.g. owner decides D15) → move the row to the right group, keep the ID.
- Verify evidence lines still match before acting on a row — line numbers drift; the claim, not the line number, is the anchor.
