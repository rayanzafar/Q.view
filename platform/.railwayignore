# railway up uses this INSTEAD of .gitignore, so it must list every exclusion explicitly.
# Goal: upload the app code + the sanitized seed/*.demo.json (business structure, no salaries/IPs),
# while excluding heavy build dirs, local databases, secrets, and the REAL sensitive snapshot.
node_modules
.git
data/*.db
data/*.db-wal
data/*.db-shm
data/backups
data/outbox
data/migration-reconciliation.json
.env
# real snapshot carries salaries/IPs — never upload it (the sanitized *.demo.json is used instead).
# glob, not exact names: any new snapshot must be excluded too, not just today's two files.
seed/*.snapshot.json
# backfill files carry secrets (labeled so in .gitignore) — never upload
seed/*.backfill.json
*.log
.DS_Store

# release evidence packs are repo artifacts, not deploy payload
docs/evidence
# محاضر الاجتماعات وصندوق الوارد: مادة داخلية (نصوص اجتماعات وملفات يرسلها المالك) لا يحتاجها
# التشغيل — وهذا الملف يحلّ محل .gitignore عند الرفع، فغيابها هنا كان يرفعها ولو كانت مُتجاهَلة
# في git. أضيفت بعد أن نبّه إليها فحصٌ قبل نشر v5.40.
docs/meetings
docs/inbox
