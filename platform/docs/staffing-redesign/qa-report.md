# تقرير جودة v5.26 — مساحة عمل التسكين

التاريخ: 2026-08-12 · البيئة: نسخة QA محلية قابلة للرمي + الحزمة الكاملة على SQLite،
ثم النشر الحي على `staging.os.evcsol.com` وتحقّقه (§٥).

## ١) الاختبارات

- **الحزمة كاملة: 1650/1650** و**e2e: 98/98 (10 مواصفات)** — صفر فشل.
- جديد: `staffing-gaps` (7 — تعريف الفجوة والمقام واستثناء الشهر الحالي وحواف ISO) ·
  `staffing-months-map` (5 — خريطة أشهر بتدقيق واحد، 0 يمسح، انحدار فرعَي الخلية والمدى،
  months على الإسنادَين) · `staffing-bulk` (4 — **الذرّية: فشلٌ وسط الدفعة يترك صفر صفوف
  وصفر تدقيق**، تفويض لكل عملية، تسمية البند الفاشل، السقف) · `staffing-matrix-page`
  (6 — بنية مسطّحة، 4 بلاطات بالضبط، `?year=` يعمل، `?dept=` يضيّق فقط، فجوة «غير مسكن»
  بحتمية month، قارئ بلا أزرار كتابة، «لا أعضاء ضمن نطاقك») · `e2e/staffing.spec` (6 —
  لصوق الرأس والعمود فعلياً بعد تمرير، درج الخلية يفتح، شريط التحديد بعدّاده، «تسكين جديد»
  يبدأ بسؤال الجهة، صفر فيض 390).
- الحارسات القائمة كلها بلا تعديل: support-unit (شكل `?sector=` وقرب الاسم من وحدته)،
  internal-work (workBuckets/staffYear وأسماء البنود)، department-scope ([]نطاق أعمى)،
  staffing-edit (عقد `{month,pct}`)، governance (رسالة التكرار)، staffing-summary،
  utilization-may2026، imports-undo، confirmation، guide-page (مرساة `#staff-q` لكل دور).
- تصحيح تسلسل أثناء العمل: اختبارا العقد دُفعا قبل الview فاحمرّ CI — سُحبا مؤقتاً
  (83455f6، cadddb6) وعادا حرفياً مع إيداع الواجهة (وأُصلحت في مواصفة e2e قياسُ ظهور
  شريطٍ position:fixed عبر المستطيل لا offsetParent).

## ٢) الجولة البصرية (Playwright · chromium · 1440 + 390 · admin/sectorlead)

14 لقطة في جولة البناء (fix-loop حي): المصفوفة الافتراضية · فلتر الفجوات · درج الخلية ·
درج الموظف · «تسكين جديد» بمراجعته الحية وتحذير 130/230% · وضع التحديد بشريطه ومعاينته —
كلها **صفر فيض أفقي وصفر أخطاء console**. عيبان صيدا بصرياً وأُصلحا قبل التسليم
(قاعدة `.mgrid{display:flex}` تغلب hidden؛ خصوصية `.field .input` تمدّد منتقيَي المدى).

## ٣) رحلات المالك الـ17 — التغطية

مغطاة آلياً: قراءة المصفوفة (1) · سنة عبر الرابط (2) · قطاع (3) · إدارة (4) · حالة (5) ·
بحث (6) · نقرة خلية وتفصيلها (7) · تعديل شهر واحد (8) · تطبيق على عدة أشهر (9) · إضافة
جهة من الخلية (10) · بنود درج الموظف كعمليات (11) · تسكين جديد متعدد الموظفين والمدى (12)
· نسبة لكل شهر (13) · تحذيرات المراجعة نصاً (14) · ذرّية التحديد المتعدد (15) · فجوة
فلترةً ورسماً (16) · وصول رابط مركز القطاع بالسنة والقطاع (17) — واكتمال تدفقات الأدراج
والمعاينة مثبَّت بصرياً باللقطات أعلاه.

## ٤) الانحرافات المعلنة (قاعدة عدم الاختراع)

1. لا «عمل تشغيلي» — الأنواع الحقيقية: مشروع + بنود المالك الثلاثة.
2. لا ساعات ولا توصيات ذكية ولا مهارات (طور لاحق بنص الحزمة).
3. التجاوز تحذير لا منع (قاعدة النظام؛ القصّ 150%).
4. الفجوات تُنسب للقطاع/الإدارة الحالية، ومن عُطّل حسابه لا تُرى فجواته الماضية (قيد 007).
5. viewer/external لا يبلغان الصفحة أصلاً (بوابة `PAGE_ACCESS` القائمة) — القراءة-فقط
   مثبتة بدور قارئ يبلغها فعلاً.
6. لون «تسكين قطاعي» البنفسجي سقط من المصفوفة (اللون للحالة فقط — decision-log ق٨).

## ٥) بوابة الإطلاق — مستكملة (2026-08-12)

1. **نسخة تطبيقية طازجة قبل النشر**: `backup-قبل-نشر-v5.26.ndjson` (2.7MB، عبر
   `backup-live2.mjs` ببيانات الاعتماد من متغيرات الخدمة وقت التشغيل).
2. **النشر**: `SANAD_RELEASE=1 npm run deploy -- --no-backup` (النسخة أُخذت أعلاه) —
   بوابات الجودة ثم البناء حتى حالة **SUCCESS** في قائمة النشرات.
3. **مسح KI-028 بالأدوار السبعة نظيف مرتين** (بعد التفعيل وبعد اللقطات):
   21 صفحة + 26 مسباراً لكل دور — 336 طلباً · P50 221ms · P95 319ms · أقصى 682ms ·
   صفر تسريب · صفر مصطلح محظور · كل الحالات مطابقة.
4. **لقطات حية** (Playwright · chromium · 1440×900 · مدير إدارة تجريبي):
   المصفوفة ببلاطاتها الأربع وفجواتها «— غير مسكن» · درج الخلية بتحذير الفجوة
   وإضافة بند · «تسكين جديد» بأقسامه الخمسة ومراجعته الحية —
   **صفر فيض أفقي · صفر أخطاء متصفح · 12 خلية مرسومة**.
5. علامة CHANGELOG v5.26 قُلبت إلى «منشور ومتحقَّق منه حياً».

**قرار الإطلاق: PASS.**
