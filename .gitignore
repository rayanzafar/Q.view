# session-local hook markers
.claude/.edited
.claude/.tested
.claude/settings.local.json

# جذور عمل وكلاء البناء المتوازيين — مساحات مؤقتة لا تُلتزم
.claude/worktrees/

# أسرار الجذر — لا تُرفع أبداً
.env
.env.*

# بيانات وقت التشغيل ونُسخ احتياطية في الجذر — لا تُرفع أبداً
data/
