# قائمة الجاهزية للإنتاج — سند (Production Readiness Checklist)

> تقييم أمين وصريح لحالة كل بند. **الرموز:** ✅ منجَز ومختبَر · 🟡 هيكلي/جزئي (يعمل لكن يحتاج توسعة) · ⛔ متبقٍّ · 🔐 يحتاج سرًّا/بنية من العميل عند النشر.
> هذا الإصدار **شريحة رأسية عاملة** تثبت المعمارية على بيانات حقيقية؛ ليس ادعاءً بالاكتمال الكامل.

## Gate 1 — السلامة
| البند | الحالة | ملاحظة |
|-------|:---:|--------|
| عدم المساس بالإنتاج | ✅ | لم تُجرَ أي كتابة على `os.evcsol.com`؛ البيانات مصدرها لقطة قراءة-فقط |
| نسخة احتياطية + تحقق استعادة | ✅ | `scripts/backup.js` ينسخ ويفحص `integrity_check` (اختُبر) |
| بيئة تطوير معزولة | ✅ | DB ملف مستقل؛ لا اتصال بالإنتاج |
| Rollback plan | ✅ | `docs/guides/ROLLBACK.md` (نسخ DB + وسم migration) |
| Staging منفصل | 🔐 | يحتاج بنية استضافة العميل (Railway/سحابة) — الكود جاهز للنشر |

## Gate 2 — المعمارية
| البند | الحالة | ملاحظة |
|-------|:---:|--------|
| Domain + Data Model | ✅ | `migrations/001_init.sql` (50+ جدول) + `docs/specs/01-data-model.md` |
| RBAC model | ✅ | `core/rbac` + `docs/specs/02-rbac.md`؛ 16 دورًا، resource×action×scope |
| API contract | ✅ | `modules/api.routes.js` + `docs/specs/03-api-contract.md` |
| ADRs | ✅ | `docs/adr/ADR-0001, ADR-0002` |

## Gate 3 — الأمان
| البند | الحالة | ملاحظة |
|-------|:---:|--------|
| تفويض على الخادم لكل مسار | ✅ | `requirePermission` + `can()` داخل كل خدمة؛ لا قرار في المتصفح |
| حجب الحقول الحساسة خادميًا | ✅ | رواتب/تكلفة/هوامش/IP — مختبَر بـ 9/9 (bd لا يستلم القيمة أصلًا) |
| تقييد النطاق (قطاع/مشروع/ذاتي) | ✅ | `scopeFilter` في الاستعلامات + مختبَر |
| مصادقة (scrypt/قفل/جلسات) | ✅ | `core/auth`؛ HttpOnly, lockout, must_change_pw |
| CSRF/تحصين الجلسة | 🟡 | كوكي SameSite=lax + secure في الإنتاج؛ توكن CSRF للنماذج متبقٍّ |
| اختبار اختراق مستقل | ⛔ | متبقٍّ (مقترح جولة قبل الإطلاق) |
| إخفاء/تنقيح IP في الواجهات | ✅ | IP معلّم حساسًا؛ لا يُعرض لغير الأدمن |

## Gate 4 — الجودة
| البند | الحالة | ملاحظة |
|-------|:---:|--------|
| اختبارات أمن/صلاحيات/حجب | ✅ | `tests/security.test.js` 9/9 |
| اختبارات وحدة (مال/تشفير) | ✅ | ضمن نفس المجموعة |
| اختبارات تكامل HTTP شاملة | 🟡 | تحقق يدوي عبر curl لكل الأدوار؛ أتمتة HTTP كاملة متبقية |
| CI pipeline | ⛔ | متبقٍّ |

## Gate 5 — الترحيل
| البند | الحالة | ملاحظة |
|-------|:---:|--------|
| Dry-run + reconciliation | ✅ | `data/migration-reconciliation.json`؛ الأعداد تطابق المصدر |
| عدم فقد بيانات مالية | ✅ | أُصلح إسقاط سطر إيراد 30.7M؛ الآن يُحفظ ويُوسم (بلا فقد) |
| تطابق المجاميع المالية | ✅ | إيراد 61,188,430 وقيمة فرص 1,279,326,694 تطابقان المصدر |
| كلمات مرور المستخدمين | 🔐 | غير موجودة في اللقطة → تُضبط بعد الترحيل |

## Gate 6 — الإطلاق
| البند | الحالة | ملاحظة |
|-------|:---:|--------|
| دليل نشر + تراجع | ✅ | `docs/guides/DEPLOYMENT.md`, `ROLLBACK.md` |
| UAT بحسابات كل دور | ✅ | 10 حسابات تجريبية عاملة (`npm run seed`) |
| Postgres للإنتاج | 🟡 | طبقة المستودعات مهيّأة؛ محوّل Postgres يُفعَّل عبر `DATABASE_URL` |
| بريد SMTP فعلي | 🔐 | Transport «preview» في التطوير؛ SMTP يحتاج أسرار العميل |
| المساعد الذكي | 🔐 | معطّل حتى يُزوَّد `OPENAI_API_KEY`؛ البنية المحوكمة (preview/audit/scope) مصمّمة |

## القدرات الوظيفية — حالة التنفيذ
| القدرة | الحالة |
|--------|:---:|
| الهيكل التنظيمي المرن (Company→Sector→Dept→Unit→Team→Position→Employee) | ✅ schema + ترحيل؛ 🟡 شاشات الإدارة الكاملة |
| CRM/الفرص + المراحل + التاريخ | ✅ خدمة + واجهة + اختبار |
| PMO: مشاريع/مهام/Quick-Add/مخرجات/مخاطر | ✅ مشاريع+مهام+Quick-Add عامل؛ 🟡 Gantt/Timeline |
| Timesheets: إدخال/تحقق/إشغال/اعتماد | ✅ عامل؛ 🟡 Timer حي في الواجهة |
| Workflow/الاعتمادات متعدد الخطوات بحدود | ✅ محرك عامل + إشعارات |
| المالية: عقود/فواتير/تحصيل/مصروفات | ✅ schema + اشتقاق ترحيلي؛ 🟡 شاشات كاملة + تكامل ERP |
| التقارير + البريد التنفيذي + الجدولة | ✅ محرك + قالبان + طابور + معاينة؛ 🟡 بقية القوالب الستة |
| KPI Framework | ✅ تعريفات + حسابات PMO؛ 🟡 لوحات KPI كاملة |
| التدقيق + الإشعارات | ✅ عامل |
| المساعد الذكي المحوكم | 🟡 بنية + سياسة؛ 🔐 يحتاج مفتاح |
