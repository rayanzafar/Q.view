---
description: Assess production go-live readiness — checks everything runnable, and lists ONLY genuinely external blockers with their exact owner-action.
---
Produce/refresh `platform/docs/PRODUCTION-READINESS.md` by CHECKING (not assuming) each area on the current staging deployment: migrations idempotent on live PG; backup/restore actually drilled (pg_dump + restore to scratch DB + row-count match); rollback drilled (previous deployment restored once); health/ready endpoints; scheduler running (report_schedule fires, email_queue drains); security headers present (curl -I); rate limit trips; permissions matrix green; sweep green; evidence pack fresh; secrets absent from image (`railway run env` review) and from git history for new files. Then split findings into: (a) DONE — verified with evidence, (b) TODO in-repo — do them now, (c) **EXTERNAL BLOCKERS ONLY** — each with the exact action for the owner (e.g. "GitHub App write permission", "SMTP credentials for @evcsol.com", "Railway Account Token for backups", "SST Arabic license", "file storage bucket"). The final report to the owner contains ONLY list (c) plus the verification evidence summary.
